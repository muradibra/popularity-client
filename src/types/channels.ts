export type TChannel = {
  stream_id: number;
  stream_name: string;
};

export type TTopChannel = {
  picture_url: string;
  stream_name: string;
  video_count: number;
  youtube_link: string;
  youtube_subscribers: number;
};
